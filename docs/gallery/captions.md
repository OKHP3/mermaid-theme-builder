# Product Hunt Gallery — Captions & Alt Text

Screenshots for the Product Hunt listing gallery.
Captured at 1280×800. All images saved as PNG.

---

## 01 — Apply Tab, GitHub Renderer, Init Directive

**Filename:** `01-apply-palette-github-init.png`

**Caption:**
> Renderer-aware export: pick GitHub, Obsidian, or mermaid.live — get profiled code

**Alt text:**
> Mermaid Theme Builder Apply tab with the OKHP3 brand palette selected, GitHub renderer
> chosen from the Target dropdown, a flowchart rendered with the dark navy-and-orange
> brand theme, and the %%{init}%% format button active in the export toolbar.

---

## 02 — Apply Tab, Export Preview Open, Directive-Length Advisory

**Filename:** `02-apply-export-preview-advisory.png`

**Caption:**
> Export preview with advisory: see exact output before you copy

**Alt text:**
> Apply tab export preview pane open showing the full %%{init}%% directive output with
> an amber advisory banner reading "%%{init}%% directive exceeds GitHub's ~500-char limit
> — validate before publishing, or switch to YAML frontmatter."

---

## 03 — Compose Tab, Live Branded Diagram

**Filename:** `03-compose-branded-diagram.png`

**Caption:**
> Live branded preview: theme a flowchart, sequence, or ER diagram in real time

**Alt text:**
> Mermaid Theme Builder Compose tab showing a live flowchart rendered with the OKHP3
> brand palette (dark navy nodes, orange accents), with the Colors accordion expanded
> and palette swatches visible in the left panel alongside a real-time diagram preview.

---

## 04 — Extract Tab, Pasted Code, Extracted Palette Variables

**Filename:** `04-extract-palette-variables.png`

**Caption:**
> Extract: reverse-engineer any Mermaid theme from code you didn't write

**Alt text:**
> Extract tab with a Mermaid flowchart containing an embedded %%{init}%% theme block
> pasted in the input area. Below it, 15 extracted theme variables are shown as colour
> swatches (Primary, Primary text, Primary border, Secondary, etc.) alongside a live
> re-themed diagram preview.

---

## 05 — Reference Tab, Renderer × Family Capability Matrix

**Filename:** `05-reference-capability-matrix.png`

**Caption:**
> 31 diagram families × 5 renderers: see exactly which variables have effect where

**Alt text:**
> Reference tab Renderer Parity Matrix section expanded to show an 8-renderer ×
> capability table. Rows list mermaid.live, GitHub, GitLab, Notion, Obsidian,
> Confluence, CLI/mmdc, and M365/Loop. Columns show Classic look, Neo look,
> Hand-Drawn look, init directive, themeVars, classDef, CSS, and fonts support,
> each cell colour-coded Full (green), Partial (amber), or None (red).
