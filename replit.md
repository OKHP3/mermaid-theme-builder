# Workspace

## Overview

pnpm workspace monorepo using TypeScript. Single deployable artifact: a static React + Vite app. No backend, no database, no API.

## Stack

- **Monorepo tool**: pnpm workspaces
- **Node.js version**: 24
- **Package manager**: pnpm
- **TypeScript version**: 5.9
- **Build**: Vite (static output to `dist/public/`)

## Artifacts

### Mermaid Theme Builder (`artifacts/mermaid-theme-builder`)

- **Type**: react-vite, fully static frontend — no backend, no login, no API calls
- **Preview path**: `/mermaid-theme-builder`
- **Tech**: React, Vite, Tailwind CSS, Mermaid.js
- **Purpose**: Visual theme builder for Mermaid diagrams generated by humans or AI

**Features:**
- Paste Mermaid code → auto-detects diagram family (flowchart, sequence, class, ER, Gantt, pie, etc.)
- Built-in brand palettes: Overkill Hill, AskJamie, Glee Fully, and utility palettes
- Two-way live color editor — click color swatches to update, preview updates instantly
- Side-by-side live preview (Original vs Themed tabs)
- Generates `%%{init:...}%%` directive-based themed output
- Render-safety warnings (existing init directives, non-printable chars, long labels)
- Three exports: Styled Code, Markdown Bootstrap, Prompt Scaffold
- Extract mode: pull theme from existing themed Mermaid code
- Family-specific overrides (familyTheming.ts)
- GitHub Pages compatible (pure static build)

**Key source files:**
- `src/lib/palettes.ts` — palette definitions and BUILTIN_PALETTES
- `src/lib/detector.ts` — diagram family detection
- `src/lib/themeEngine.ts` — theme application and export generation
- `src/lib/extractor.ts` — extract theme from existing Mermaid code
- `src/lib/familyTheming.ts` — per-diagram-family theme overlays
- `src/lib/persistence.ts` — localStorage state, shareable URL encoding
- `src/lib/diff.ts` — diff view between original and themed code
- `src/lib/diagramSplit.ts` — multi-diagram splitting
- `src/lib/exporters.ts` — file download, SVG/PNG render, JSON import/export
- `src/lib/themeMode.ts` — light/dark/system mode
- `src/data/mermaid-capabilities.ts` — diagram capability registry
- `src/data/examples.ts` — palette preview diagrams (BRAND_EXAMPLES, SHOWCASE_EXAMPLE)
- `src/data/example-library.ts` — browse catalogue (EXAMPLE_CATALOG, EXAMPLE_GROUPS)
- `src/components/MermaidPreview.tsx` — live Mermaid renderer
- `src/components/ColorSwatch.tsx` — two-way color editor
- `src/pages/ThemeBuilder.tsx` — main UI shell

**Authoring references (`examples/`):**
- 26 `.mmd` source files used as authoring references for diagram content inlined into `examples.ts` and `example-library.ts`
- Not included in the build; dev/authoring use only

### Mockup Sandbox (`artifacts/mockup-sandbox`)

- Replit Canvas platform tool for UI prototyping on the canvas board
- `kind = "design"`, preview path `/__mockup`
- Currently idle (no active mockups registered)

## Key Commands

- `pnpm --filter @workspace/mermaid-theme-builder run typecheck` — typecheck the app
- `PORT=18624 BASE_PATH=/mermaid-theme-builder pnpm --filter @workspace/mermaid-theme-builder run build` — production build

## Post-merge

`scripts/post-merge.sh` — runs `pnpm install --frozen-lockfile` after every task agent merge.

See the `pnpm-workspace` skill for workspace structure and TypeScript setup.
