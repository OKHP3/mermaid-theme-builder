# Deprecated — superseded by deploy-pages.yml since v0.3-alpha. Safe to delete.
name: Deploy Mermaid Theme Builder (deprecated)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated. See deploy-pages.yml."
